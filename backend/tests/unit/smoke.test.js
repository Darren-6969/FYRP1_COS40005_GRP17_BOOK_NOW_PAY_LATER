import { test } from 'node:test';
import assert from 'node:assert/strict';

// Placeholder so `npm run test` is a real command from day one rather than
// something the definition of done points at and nobody can run. Delete this
// once BNPL-004 lands genuine unit tests.
test('unit test runner is wired up', () => {
  assert.equal(1 + 1, 2);
});
